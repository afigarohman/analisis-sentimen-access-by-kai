# Development (npm run dev):
#   Jangan set VITE_API_BASE_URL — axios memakai /api lewat proxy Vite ke http://127.0.0.1:8000
#   (mengatasi blokir browser/firewall ke alamat 127.0.0.1).
#
# Production (build):
VITE_API_BASE_URL=http://127.0.0.1:8000
